<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".GitHubApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/github"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.gitDemo"
        tools:targetApi="34">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.gitDemo"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="mygithubos"
                    android:host="oauth"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.screens.create_issue.CreateIssueActivity"
            android:exported="false"
            android:label="创建Issue"
            android:theme="@style/Theme.gitDemo"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:exported="false"
            android:label="登录"
            android:theme="@style/Theme.gitDemo" />
        <activity
            android:name=".ui.activity.MainActivityXml"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.gitDemo" />
        <activity
            android:name=".ui.activity.RepoDetailsActivity"
            android:exported="false"
            android:label="@string/project_details"
            android:theme="@style/Theme.gitDemo"
            android:parentActivityName=".ui.activity.MainActivityXml" />
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.gitDemo"
            android:parentActivityName=".ui.activity.MainActivityXml" />
    </application>

</manifest>